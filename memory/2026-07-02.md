# Сводка агентских изменений — 2026-07-02

## [10:20] Агент: Agent 3 — Актуализация агентских файлов (пропущенная запись A59)

**Что:** Обнаружено, что коммит `6703fde` (Teacher Analytics — Time Distribution tab with Pie Chart, 2026-06-30) был отражён в `AGENTS.md` (A59) и `AGENT_TASKS.md`, но отсутствовал в `memory/AGENTS-HISTORY.md`.

**Решение:**
- Добавлена архивная запись в `memory/AGENTS-HISTORY.md` о `6703fde`.
- Создан `memory/2026-07-02.md` — эта запись.

**Git:** `TBD`

**Сборка:** Не требуется — изменения только в агентских файлах.

**Статус:** ✅ Агентские файлы актуальны.

---

## [10:44] Агент: Agent 3 — Lesson null-safety fix (A63)

**Что:** Исправлена runtime ошибка при вызове `applyQuestionEdits`.
1. `applyQuestionEdits` теперь принимает `Question | undefined`, возвращает `undefined` для невалидных вопросов с `console.warn`.
2. `Lesson.tsx` проверяет `rawQuestion?.id` перед вызовом `applyQuestionEdits`.
3. Убраны некорректные аргументы `lessonId` и `editVersion` из вызова (функция их не принимает).

**Где:** `src/lib/questionEdits.ts`, `src/pages/Lesson.tsx`

**Git:** `38a6ca8`

**Сборка:** `npm run build` ✅ (0 TypeScript ошибок).

---

## [11:35] Агент: Agent 3 — PWA cache-busting fix (A64)

**Что:** Добавлен `manifestTransforms` в `vite.config.ts` для предотвращения stale Service Worker cache.
1. `manifestTransforms` добавляет timestamp к `revision` для `.js`, `.css` и `index.html`.
2. `cleanupOutdatedCaches: true` — удаляет устаревшие кэши при активации нового SW.
3. Исправляет ошибку "rawQuestion is not defined", вызванную тем, что старый SW отдавал закэшированный `Lesson.tsx`.

## [12:25] Агент: Agent 3 — PWA self-destruct fix (emergency) (A65)

**Что:** Отключено кэширование Service Worker'ом — `selfDestroying: true` в `vite.config.ts`.
1. `selfDestroying: true` — SW самоуничтожается при активации и очищает все кэши.
2. Браузер всегда загружает свежие JS/CSS с сервера, игнорируя SW-кэш.
3. Emergency-фикс на случай, если `manifestTransforms` (844cb9b) не сработал и stale SW продолжает отдавать старый `Lesson.tsx`.
4. Исправляет ошибку "rawQuestion is not defined", вызванную stale кэшем.

**Где:** `vite.config.ts`

**Git:** `5c81c10`

**Сборка:** `npm run build` ✅ (0 TypeScript ошибок).
