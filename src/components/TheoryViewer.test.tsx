import { describe, it, expect } from 'vitest'
import { TheoryViewer } from './TheoryViewer'

describe('TheoryViewer', () => {
  it('imports without errors', () => {
    expect(TheoryViewer).toBeDefined()
  })
})
